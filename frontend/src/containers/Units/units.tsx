import React from "react";
import { BackgroundDiv } from "../../shared/Styles/shared-styles";

interface UnitFilter {
    name: string;
    builder?: string;
    tier?: number;
    "Melee / Ranged"?: string;
    attackType?: string;
    defenseType?: string;
}

const units = () => {
// first lets pull through all the units
// then lets work on the filtering

return (
    <BackgroundDiv height="100vh"></BackgroundDiv>
)

}

export default units;